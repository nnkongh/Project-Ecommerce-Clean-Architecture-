@{
    ViewData["Title"] = "Xác nhận quên mật khẩu";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow">
            <div class="card-body p-5 text-center">
                <i class="fas fa-envelope-open-text fa-4x text-success mb-4"></i>
                <h2 class="mb-4">@ViewData["Title"]</h2>
                <p class="lead">
                    Chúng tôi đã gửi link đặt lại mật khẩu đến email của bạn.
                </p>
                <p class="text-muted">
                    Vui lòng kiểm tra hộp thư và làm theo hướng dẫn trong email.
                    Nếu không thấy email, hãy kiểm tra thư mục spam.
                </p>
                <a asp-action="Login" class="btn btn-primary mt-3">Quay lại đăng nhập</a>
            </div>
        </div>
    </div>
</div>